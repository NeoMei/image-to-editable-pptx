interface:
  display_name: "Image to Editable PPTX"
  short_description: "Turn slide images into editable PPTX files"
  default_prompt: "Use $image-to-editable-pptx to convert this slide image into a high-fidelity editable PPTX, discovering real registered OpenAI/Gemini tools before using the packaged host bridge and never inventing capabilities or model results."

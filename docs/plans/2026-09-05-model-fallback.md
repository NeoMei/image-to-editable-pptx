# Host-first model fallback implementation

## Approved scope and global constraints

The user approved the complete scope: OCR, full-page/regional scene analysis, and image completion use `host-openai`, `api-openai`, `host-gemini`, `api-gemini`, `api-alibaba` in that order. Host capabilities must be registered/callable and never depend on extracted cookies or tokens. Missing keys skip candidates. Only `unavailable`, `auth_unavailable`, and `retryable_exhausted` advance; `policy_refused`, `invalid_input`, `invalid_output`, and `local_failure` stop. Keep independent forward-only sticky state for OCR, scene, and completion, serializing host work. Validate outputs before marking success. No previously successful operation is regenerated. API credentials stay environment-only; models are configurable without exposing secrets. Preserve offline v2 build, legacy v1 replay, source aspect ratio, strict analysis hashes, ownership, safe publication, and visual fidelity checks. No push, merge, release, or installed-plugin replacement in this task.

Reference implementation (read-only): `/Users/neomei/项目/codexprojects/ai-image-to-ppt/references/host-image-routing.md` and `scripts/host_routing_policy.py`.

## Task 1: Routing kernel and callable host bridge

Implement focused `src/providers/routing.ts` and `src/providers/host-bridge.ts` with corresponding offline tests. Do not edit pipeline/config/CLI yet.

Expose candidate/operation types, classified ProviderFailure, and a serial per-operation sticky router. Its executor must return a validated result with the actual model identity. Return/report ordered candidate statuses without raw provider error strings; missing candidates and exhaustion must be explicit. Unknown executor exceptions are local_failure, never retryable. Reports include operation sequence, selected candidate/model and each attempt; a fatal/exhausted operation stops the router. Prevent concurrent interleaving. Permit separate router instances for separate runs.

Define host requests containing operation (`ocr`, `scene`, `completion`), prompt, PNG image Buffer, canvas dimensions, optional hidden/protected masks. Define host results as model plus text or image bytes, and failures using the classified error. Model labels are bounded safe identifiers.

Implement a file bridge usable by an actual CLI and Skill: an explicitly supplied existing private bridge directory contains a validated `capabilities.json` declaring which host provider supports which operations. Missing capabilities are unavailable immediately. Each invocation creates a unique private request directory, writes request JSON and PNG inputs, and waits boundedly for response JSON with matching request ID. The Skill calls an actual host tool and writes that response atomically. Responses support classified failure, text success, or one absolute local image path. Reject symlinks/nonregular/oversized responses and artifacts, mismatched request IDs, malformed JSON, ambiguous output, and HTTP URLs. Read image bytes once and let the operation adapter validate dimensions/content before routing success. Do not fetch host URLs. Include injectable short polling/timeout for tests and a practical bounded production timeout (ten minutes). No shell commands or executable hooks in manifests. Export a straightforward interface for Task 2 and document it in your report.

Tests must prove order, skipped credentials/capabilities, sticky forward movement, per-operation independence, fatal short-circuit, exhaustion redaction, serialized calls, successful file bridge handoff, missing capability, timeout, malformed/mismatched/symlink/oversized responses, and bounded local image import. Use failing tests first, then implementation. Run focused tests, type checks, full tests once. Commit only your scoped files.

## Task 2: Provider adapters and pipeline integration

Use Task 1's interfaces to implement OpenAI and Gemini OCR/scene/image completion adapters, existing Alibaba as final fallback, configuration, CLI bridge wiring, and pipeline integration. Add focused modules; keep existing provider entrypoints compatible where possible.

Default OpenAI analysis model is `gpt-4.1`; Gemini analysis is `gemini-2.5-flash`; image models match the reference plugin (`gpt-image-2`, `gemini-3.1-flash-image`). Support explicit model environment overrides. Keep Alibaba defaults. Verify API request/response shapes with official documentation. Use official fixed endpoints and no redirects; keys go only to their own provider. Avoid adding SDK dependencies when built-in fetch/FormData suffice. Correctly distinguish refusals, authentication, absent models, transient transport/429/5xx, invalid JSON/output and local observer failures. Bounded retries, no fallback on explicit safety refusal.

OCR must produce existing validated pixel-coordinate OcrResult with finite in-canvas boxes. Use existing scene prompt/parser contracts for all providers. Completion uses original crop plus masks and validates image dimensions before existing protected-visible/recomposition checks; generated image size adaptation may preserve aspect ratio but must not stretch/crop incompatible geometry. Invalid output must not silently advance. Do not remove existing safe quality fallback for semantically unextractable objects.

Make Alibaba credentials lazy/optional so OpenAI, Gemini or host-only runs work without DASHSCOPE variables. Preserve strict legacy loadConfig API if needed; introduce routing configuration separately. CLI `analyze` and `run` accept `--host-bridge <dir>`; offline build/build-v1 reject this and remain credential/network-free. CLI-only runs skip unavailable hosts without waiting. Pipeline injected config must continue working for old tests.

Store optional versioned routing report in analysis ledger (old v2 packages remain readable), including attempted provider calls, chosen model and failure classification. Existing request counters represent logical operations and keep their established bounds; report actual fallback attempts separately. Models must reflect effective results, including regional switching. Persist safe failed-routing evidence and never secrets. Fatal routing errors must escape optional refinement/completion catches rather than being hidden as quality fallback.

Tests cover real adapters via mocked fetch, no-key host-only analyze-to-offline-build, API-only OpenAI/Gemini without Alibaba credentials, ordered fallback to Alibaba, independent operation state, fatal refusal/invalid output, image geometry, effective ledger identity, old package compatibility, failed-run retention, offline command rejection. Run focused tests then repository verification. Commit scoped changes.

## Task 3: Plugin guidance and end-to-end acceptance

Update README, packaged SKILL, agent metadata where relevant, and add a concise host-routing reference/capability manifest describing the shipped protocol exactly. Provide copyable host capability manifest and request/response examples, a concrete Skill loop that services the running CLI with registered tools, skips absent capabilities, never invents model results, and writes atomic responses. Explain optional provider keys/model defaults, route order, per-operation stickiness, fatal boundaries, logical vs actual request counts, and offline build. Keep the original image/PPTX validation and publication requirements. Add a true child-process CLI bridge test: launch analyze with no provider credentials, service at least OCR+scene requests via the documented file protocol, then build offline from its package. Verify network is not required and outputs are real artifacts. Cover host failure advancing to API in adapter-level tests if child process cannot safely fake external services.

Run npm run verify and npm pack --dry-run once after changes; distinguish fixture-based integration proof from real provider acceptance. Do not use user private slides or spend provider credits for acceptance without a suitable provided source. Commit docs/tests. Finish with task review and independent whole-branch review.
